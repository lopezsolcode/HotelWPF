<Window x:Class="HotelManagementSystem.Views.AgregarHabitacion"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:HotelManagementSystem.Views"
        mc:Ignorable="d"
        Title="AgregarHabitacion" Height="350" Width="800"  WindowStartupLocation="CenterScreen"  AllowsTransparency="True" WindowStyle="None" ResizeMode="NoResize" Background="Transparent">
    <Grid Background="Transparent">
        <Border Background="WhiteSmoke" BorderBrush="#eff2f7" BorderThickness="1" CornerRadius="30"  MouseDown="Border_MouseDown" Margin="0 0 0 0" >
            <Grid Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="100" />
                    <RowDefinition Height="*" />

                </Grid.RowDefinitions>
                <TextBlock  Grid.Row="0" Text="Agregar Habitación" FontSize="32" FontWeight="SemiBold"
                                          VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#121518">
                </TextBlock>

                <Button Width="30" Height="30" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="10 20 20 10" Click="Delete_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#d32f2f"/>
                            <Setter Property="BorderBrush" Value="#b71c1c"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#f44336"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <materialDesign:PackIcon Kind="Close" Width="12" Height="12" VerticalAlignment="Center"/>
                </Button>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Número de habitación:" Grid.Row="0" Grid.Column="0" FontSize="15" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" Margin="10 10 10 10" VerticalAlignment="Top"></TextBlock>

                    <TextBox x:Name="txtNoHabitacion" Grid.Row="0" Grid.Column="0"  Margin="100 10 20 10" Padding="10 7" Background="#ffffff" Foreground="#b0b9c6" BorderThickness="0" FontSize="15"  VerticalAlignment="Bottom" Height="36">
                        <TextBox.Template>
                            <ControlTemplate TargetType="TextBoxBase">
                                <Border x:Name="border" CornerRadius="7" Background="{TemplateBinding Background}" SnapsToDevicePixels="True" BorderThickness="1" BorderBrush="#e0e6ed">
                                    <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="#d9d9d9" TargetName="border"/>
                                    </Trigger>

                                    <Trigger Property="IsKeyboardFocused" Value="True">
                                        <Setter Property="BorderBrush" Value="#d9d9d9" TargetName="border"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </TextBox.Template>
                    </TextBox>

                    <TextBlock Text="Tipo de Habitación:" Grid.Row="0" Grid.Column="1" FontSize="15" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" Margin="10 10 10 10" VerticalAlignment="Top"></TextBlock>

                    <ComboBox x:Name="txtTipoHabitacion"
                              Grid.Row="0"
                              Grid.Column="1"
                              Margin="100 10 20 10"
                              Padding="10 7"
                              Background="#ffffff"
                              Foreground="#121518"
                              BorderThickness="0"
                              FontSize="15"
                              VerticalAlignment="Bottom"
                              Height="36">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="ComboBox">
                                <Grid>
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="10"
                                            SnapsToDevicePixels="True" />   

                                    <ToggleButton Grid.Column="2"
                                                  Name="ToggleButton"
                                                  Template="{StaticResource ComboBoxToggleButton}"
                                                  Focusable="False"
                                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press"
                                                  HorizontalAlignment="Right"
                                                  Background="Transparent" />

                                    <ContentPresenter Margin="10,0,30,0"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left" />

                                    <Popup x:Name="PART_Popup"
                                           Placement="Bottom"
                                           Width="{TemplateBinding ActualWidth}"
                                           IsOpen="{TemplateBinding IsDropDownOpen}"
                                           AllowsTransparency="True"
                                           Focusable="False"
                                           PopupAnimation="Slide">      
                                        <Border Margin="1"
                                                Background="White"
                                                BorderBrush="Gray"
                                                BorderThickness="1"
                                                CornerRadius="10">
                                            <ScrollViewer Margin="4"
                                                          SnapsToDevicePixels="True">
                                                <ItemsPresenter />
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>

                        <!-- Opciones -->
                        <ComboBoxItem Content="Single"/>
                        <ComboBoxItem Content="Double"/>
                        <ComboBoxItem Content="Twin"/>
                        <ComboBoxItem Content="Triple"/>
                        <ComboBoxItem Content="Queen"/>
                        <ComboBoxItem Content="King"/>
                    </ComboBox>
                    
                    <TextBlock Text="Tipo de Cama:" Grid.Row="1" Grid.Column="0" FontSize="15" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" Margin="10 10 10 10" VerticalAlignment="Top"></TextBlock>
                    <ComboBox x:Name="txtTipoDeCama"
                              Grid.Row="1"
                              Grid.Column="0"
                              Margin="100 10 20 10"
                              Padding="10 7"
                              Background="#ffffff"
                              Foreground="#121518"
                              BorderThickness="0"
                              FontSize="15"
                              VerticalAlignment="Bottom"
                              Height="36">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="ComboBox">
                                <Grid>
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="10"
                                            SnapsToDevicePixels="True" />

                                    <ToggleButton Grid.Column="2"
                                                  Name="ToggleButton"
                                                  Template="{StaticResource ComboBoxToggleButton}"
                                                  Focusable="False"
                                                  IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press"
                                                  HorizontalAlignment="Right"
                                                  Background="Transparent" />

                                    <ContentPresenter Margin="10,0,30,0"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left" />

                                    <Popup x:Name="PART_Popup"
                                           Placement="Bottom"
                                           Width="{TemplateBinding ActualWidth}"
                                           IsOpen="{TemplateBinding IsDropDownOpen}"
                                           AllowsTransparency="True"
                                           Focusable="False"
                                           PopupAnimation="Slide">
                                        <Border Margin="1"
                                                Background="White"
                                                BorderBrush="Gray"
                                                BorderThickness="1"
                                                CornerRadius="10">
                                            <ScrollViewer Margin="4"
                                                          SnapsToDevicePixels="True">
                                                <ItemsPresenter />
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                        <ComboBoxItem Content="Standard"></ComboBoxItem>
                        <ComboBoxItem Content="Deluxe"></ComboBoxItem>
                        <ComboBoxItem Content="Suite"></ComboBoxItem>
                        <ComboBoxItem Content="Presidencial"></ComboBoxItem>
                    </ComboBox>

                    <TextBlock Text="Precio:" Grid.Row="1" Grid.Column="1" FontSize="15" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" Margin="10 10 10 10" VerticalAlignment="Top"></TextBlock>
                    <TextBox x:Name="txtPrecio" Grid.Row="1" Grid.Column="1"  Margin="100 10 20 10" Padding="10 7" Background="#ffffff" Foreground="#b0b9c6" BorderThickness="0" FontSize="15"  VerticalAlignment="Bottom" Height="36">
                        <TextBox.Template>
                            <ControlTemplate TargetType="TextBoxBase">
                                <Border x:Name="border" CornerRadius="7" Background="{TemplateBinding Background}" SnapsToDevicePixels="True" BorderThickness="1" BorderBrush="#e0e6ed">
                                    <ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="#d9d9d9" TargetName="border"/>
                                    </Trigger>

                                    <Trigger Property="IsKeyboardFocused" Value="True">
                                        <Setter Property="BorderBrush" Value="#d9d9d9" TargetName="border"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </TextBox.Template>
                    </TextBox>



                    <Button Grid.Column="1" Grid.Row="3"
                       Height="35"
                       Width="150"
                       HorizontalAlignment="Right"
                       FontSize="13"
                       Background="#6741d9"
                       Margin ="0 0 20 0"
                       Foreground="#ffffff"
                       FocusVisualStyle="{x:Null}"  
                       Click="Add_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#6741d9"/>
                                <Setter Property="Foreground" Value="#ffffff"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="7">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <!-- Aquí van los triggers -->
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#7b5cd6"/>
                                        <Setter Property="Foreground" Value="#ffffff"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">

                            <TextBlock Text="Agregar habitación" FontSize="14" Foreground="#ffffff" VerticalAlignment="Stretch"/>

                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
